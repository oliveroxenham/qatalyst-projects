export enum Status {
  Red = 0,
  Amber = 1,
  Green = 2,
}

export enum ProjectFinancialStatus {
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  ELIGIBLE = 'eligible',
  NOT_ELIGIBLE = 'not_eligible',
}

export enum ProjectEsgStatus {
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum ProjectKycStatus {
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  LOW = 'low',
  HIGH = 'high',
}

export enum GenerateAssessmentStatus {}
